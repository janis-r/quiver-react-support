{
  "name": "quiver-react-support",
  "version": "0.2.4",
  "description": "React support package for quiver-framework",
  "main": "dist/es5/index.js",
  "jsnext:main": "dist/esnext/index.js",
  "typings": "dist/es5/index.d.ts",
  "scripts": {
    "test": "mocha -r ts-node/register test/Suites.ts",
    "transpile": "tsc && tsc -p . --outDir build/dist/esnext --module es2015 --target esnext",
    "prepare-build": "npm run test && rimraf build && npm run transpile && copy README.md build",
    "publish-prepatch": "npm run prepare-build && npm version prepatch && ts-node export-package && npm publish build --tag dev",
    "publish-patch": "npm run prepare-build && npm version patch    && ts-node export-package && npm publish build",
    "publish-minor": "npm run prepare-build && npm version minor    && ts-node export-package && npm publish build",
    "publish-major": "npm run prepare-build && npm version major    && ts-node export-package && npm publish build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/janis-r/quiver-react-support"
  },
  "keywords": [
    "react",
    "dependency injection",
    "command map",
    "mediator map",
    "event dispatcher"
  ],
  "author": "Jānis Radiņš",
  "license": "ISC",
  "dependencies": {
    "quiver-framework": "^0.7.1",
    "react": "^16.0.0"
  },
  "devDependencies": {
    "@types/chai": "3.5.2",
    "@types/enzyme": "^3.1.11",
    "@types/enzyme-adapter-react-16": "^1.0.2",
    "@types/mocha": "2.2.41",
    "chai": "3.5.0",
    "copy": "0.3.0",
    "enzyme": "^3.3.0",
    "enzyme-adapter-react-16": "^1.1.1",
    "mocha": "^5.2.0",
    "mocha-typescript": "1.0.23",
    "react-dom": "^16.0.0",
    "rimraf": "^2.6.1",
    "ts-node": "^7.0.0",
    "typescript": "^3.1.1"
  }
}
